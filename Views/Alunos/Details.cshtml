@model GerenciamentoMensalidade2.Models.Alunos

@{
    ViewData["Title"] = "Detalhes do Aluno";
}

<div class="container mt-4">
    <!-- Cabeçalho com título e botão de voltar -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="mb-0">@ViewData["Title"]</h1>
        <a asp-action="Index" class="btn btn-outline-secondary btn-lg">
            <i class="bi bi-arrow-left me-1"></i> Voltar
        </a>
    </div>

    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card shadow">
                <!-- Cabeçalho do Card -->
                <div class="card-header bg-light">
                    <h5 class="mb-0">Informações do Aluno</h5>
                </div>

                <!-- Corpo do Card com detalhes -->
                <div class="card-body">
                    <dl class="row mb-0">
                        <dt class="col-sm-4">@Html.DisplayNameFor(m => m.Nome)</dt>
                        <dd class="col-sm-8">@Model.Nome</dd>

                        <dt class="col-sm-4">@Html.DisplayNameFor(m => m.Cpf)</dt>
                        <dd class="col-sm-8">@Model.Cpf</dd>

                        <dt class="col-sm-4">@Html.DisplayNameFor(m => m.Telefone)</dt>
                        <dd class="col-sm-8">@Model.Telefone</dd>

                        <dt class="col-sm-4">@Html.DisplayNameFor(m => m.Plano)</dt>
                        <dd class="col-sm-8">@Model.Plano.PlanoId</dd>

                        <dt class="col-sm-4">@Html.DisplayNameFor(m => m.Status)</dt>
                        <dd class="col-sm-8">@Model.Status</dd>
                    </dl>
                </div>

                <!-- Rodapé do Card com botões -->
                <div class="card-footer bg-white text-end">
                    <a asp-action="Edit"
                       asp-route-id="@Model.AlunosId"
                       class="btn btn-warning btn-lg me-2">
                        <i class="bi bi-pencil-square me-1"></i> Editar
                    </a>
                    <a asp-action="Index" class="btn btn-outline-secondary btn-lg">
                        <i class="bi bi-arrow-left me-1"></i> Voltar
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
